 <div class="navbar navbar-fluid-top">
    <div class="navbar-inner">
      <div class="container-fluid">
        <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>
        <a class="brand" href="/home/"><%= image_tag '/images/logo.jpg', :width => 30 %>
        <% if user_signed_in? %>Opine<%else%>Opine<%end%></a>
        <div class="container-fluid nav-collapse">
          <% if user_signed_in? %>
            <ul class="nav pull-right">
                <li class="dropdown">
                    <a href="#" role="button" class="dropdown-toggle" data-toggle="dropdown"> <i class="icon-user"></i> <%= current_user %><i class="caret"></i>

                    </a>
                    <ul class="dropdown-menu">
                        <li class="divider"></li>
                          <li>
                            <%= link_to destroy_user_session_path, method: :delete, role: 'button' do %>
                            <i class="icon-off"></i> Sair
                          <% end %>
                        </li>
                    </ul>
                </li>
            </ul>
          <% end %>
        </div><!--/.nav-collapse -->
      </div>
    </div>
  </div>
